#if 0
#ifndef DIRECTOR_INCLUDED
#define DIRECTOR_INCLUDED

class Director
{
public:

	void NotifyArrive(from, to, chit);
	void NotifyDelete(at, chit);

	sendTo ShouldSendHerd(herd, size, from);
	sendTo ShouldSendGreater(from, greater);

};

#endif // DIRECTOR_INCLUDED
#endif